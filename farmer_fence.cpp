#include<iostream>
#include<vector>
#include<algorithm>
bool isvalid(long long t, const vector<long long>&h,long long c) {
           
}

using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,c;
    cin >> n >> c;
    vector<int>arr(n);
    for(int i=0; i<n; i++) {
        cin >> arr[i];
    }
    

  
   


    return 0;
}